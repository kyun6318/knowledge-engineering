# 신선도 컬럼 검증 보고서

> 생성일: 2026-03-03
> 데이터: ClickHouse `resume.resume` (import_resume_hub_tmp)
> 총 레코드: 8,018,110건
> 목적: `updated_at` / `user_updated_at` / `indexed_at` 세 컬럼의 실제 의미와 신선도 기준 확립

---

## 스키마 COMMENT 정의

| 컬럼 | COMMENT | 의미 |
|---|---|---|
| `updated_at` | 수정일(유저) | 유저가 이력서 콘텐츠를 직접 수정한 날짜 |
| `user_updated_at` | 회원 수정일시 | 이력서 콘텐츠 + 계정 레벨 변동 모두 반영 |
| `indexed_at` | 데이터 변경일 | 시스템 데이터 파이프라인이 인덱싱한 날짜 |

---

## 기본 통계 비교

| 컬럼 | 최솟값 | 최댓값 | 중앙값 경과일 |
|---|---|---|---|
| `updated_at` | 2001-11-01 | 2026-02-25 | **1,097일** (약 3년) |
| `user_updated_at` | 2001-11-01 | 2026-02-25 | **1,029일** (약 2.8년) |
| `indexed_at` | 2026-01-14 | 2026-02-25 | **43일** |

`indexed_at`은 2026년 1~2월에만 집중되어 있으며 이는 데이터 파이프라인의 신규 배치 인덱싱 시점을 반영하는 시스템 메타데이터다. `updated_at`과 `user_updated_at`은 2001년부터 이력이 존재하여 실제 이력서 수정 이력을 담는다.

---

## 컬럼별 신선도(활성도) 비교

| 기준 컬럼 | 90일 활성 | 1년 활성 | 5년 이상 방치 |
|---|---|---|---|
| `updated_at` (이력서 직접 수정) | **11.56%** (927,108건) | 25.96% | 33.82% |
| `user_updated_at` (회원 활동 포함) | **13.92%** (1,116,450건) | 28.20% | 31.63% |
| ~~`indexed_at`~~ (시스템 인덱싱) | ~~100%~~ | ~~100%~~ | ~~0%~~ |

n = 8,018,110. 분석 기준일: 2026-03-03.

`indexed_at` 기준 100% 활성은 2026-01~02 배치 인덱싱 이벤트의 결과이며, 신선도를 전혀 반영하지 않는다. `user_updated_at` 기준 13.92%가 기존 보고서 "90일 활성 13.8%"와 일치함을 확인했다.

---

## 컬럼 간 시간차 분포

| 비교 쌍 | 평균 차이(일) | 중앙값 차이(일) |
|---|---|---|
| `updated_at` → `user_updated_at` | 72.5일 | **0일** |
| `updated_at` → `indexed_at` | 1,391.3일 | 1,055일 |

중앙값 차이 0일은 대부분의 레코드에서 두 컬럼이 동시에 기록됨을 의미한다. 평균 72.5일 차이는 일부 케이스에서 `user_updated_at`만 단독 업데이트되는 상황이 있음을 나타낸다.

---

## 동일 날짜 일치율

| 비교 쌍 | 일치 건수 | 일치율 |
|---|---|---|
| `updated_at` = `user_updated_at` (같은 날) | 5,684,795건 | **70.90%** |
| `updated_at` = `indexed_at` (같은 날) | 522,384건 | 6.52% |
| `user_updated_at` = `indexed_at` (같은 날) | 801,149건 | 9.99% |

`updated_at`과 `user_updated_at`의 70.9% 동일 기록은 동일 트랜잭션에서 함께 업데이트되는 구조를 시사한다.

---

## 시간순서 패턴

| 패턴 | 건수 | 비율 |
|---|---|---|
| `user_updated_at` > `updated_at` (회원 수정일이 더 최근) | 4,394,290건 | **54.81%** |
| `updated_at` > `user_updated_at` (이력서 수정일이 더 최근) | 2,314,664건 | 28.87% |
| `updated_at` = `user_updated_at` (완전 동일) | 1,309,156건 | 16.33% |
| `indexed_at` >= `updated_at` (인덱싱 항상 수정 이후) | 8,018,110건 | **100%** |

`user_updated_at`이 `updated_at`보다 더 최근인 케이스가 54.81%로 과반수를 차지한다. 이는 이력서 콘텐츠 수정 없이 계정 레벨 변동(프로필 수정 등)이 `user_updated_at`에만 반영되기 때문이다.

---

## `indexed_at` 월별 분포

| 월 | 건수 | 비율 |
|---|---|---|
| 2026년 1월 | 7,467,197건 | **93.13%** |
| 2026년 2월 | 550,913건 | 6.87% |

전체 이력서가 2026년 1~2월에 일괄 인덱싱된 결과다. 신규 데이터 파이프라인이 기존 전체 이력서를 배치 처리한 것으로 확인된다.

---

## 기존 보고서 수정 사항

| 항목 | 기존 해석 (오류) | 검증 후 정확한 의미 |
|---|---|---|
| `updated_at` | "시스템 자동 업데이트" | 유저의 이력서 콘텐츠 직접 수정일 |
| `user_updated_at` | "사용자 직접 수정" | 회원 수정일시 — 이력서 콘텐츠 + 계정 변동 포함 |
| 90일 활성 13.8% | 컬럼 불명확 | `user_updated_at` 기준 **13.92%** (검증 일치) |

---

## 결론 및 권장 기준

**신선도 기준: `user_updated_at` 단일 기준으로 통일**

- 스키마 COMMENT '회원 수정일시' — 회원의 실질적 활동을 반영
- 기존 보고서 수치(13.8%)와 일치하여 연속성 유지 가능
- `updated_at`보다 넓은 범위(계정 활동 포함)이지만 실제 인간 활동 기반

이력서 콘텐츠 수정만 측정할 경우 `updated_at` 사용 (90일 활성 11.56%, 더 보수적 수치).

| 기준 | 90일 활성 | 1년 활성 | 5년+ 방치 |
|---|---|---|---|
| `user_updated_at` (권장) | **13.92%** | 28.20% | 31.63% |
| `updated_at` (보수적) | 11.56% | 25.96% | 33.82% |

**`indexed_at`은 신선도 분석에서 제외** — 시스템 인덱싱 날짜로 모든 이력서가 100% 활성으로 보임.

---

**후속 작업**
- 어떤 이벤트가 `updated_at`만 업데이트하고, 어떤 이벤트가 `user_updated_at`도 함께 업데이트하는지 BE 코드 확인 (정확한 트리거 조건 명세화)
- 2001-11-01 최솟값 레코드가 실제 레거시 마이그레이션 기본값인지 확인하여 신선도 분석에서 제외 여부 결정

*Report: 2026-03-03 | Scientist Agent (claude-sonnet-4-6)*
